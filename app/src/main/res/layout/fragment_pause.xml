<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/fpause_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    android:background="@color/translucentWhite">

        <ImageButton
            android:id="@+id/fpause_btn_forward"
            android:layout_width="150dp"
            android:layout_height="match_parent"
            android:layout_alignParentRight="true"
            android:layout_alignParentEnd="true"
            android:src="@drawable/forward"
            android:padding="30dp"
            android:scaleType="fitCenter"
            android:background="@color/translucentPrimary"/>

        <ImageButton
            android:id="@+id/fpause_btn_backward"
            android:layout_width="150dp"
            android:layout_height="match_parent"
            android:layout_alignParentLeft="true"
            android:layout_alignParentStart="true"
            android:src="@drawable/backward"
            android:padding="30dp"
            android:scaleType="fitCenter"
            android:background="@color/translucentPrimary" />

    <Button
        android:id="@+id/fpause_btn_leaveworkout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="30dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:text="@string/ffeedback_leaveworkout"
        android:background="@drawable/button_unclicked"
        android:textAllCaps="false"
        android:paddingTop="11dp"
        android:paddingLeft="8dp"
        android:paddingRight="8dp"
        android:textColor="@color/white"
        android:textSize="24sp"
        android:gravity="center_horizontal"/>

</RelativeLayout>